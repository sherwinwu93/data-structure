package asymptotic_analysis1;

/**
 * Are the statements in the right column true or false? If false, correct the asymptotic
 * notation (Ω(.), Θ(.),O(.). Be sure to give the tightest bound. Ω(.) is the opposite
 * of O(.), i.e. f(n) ε Ω(g(n)) <=> g(n) ε
 */

/**
 * Give the worst case and best case runtimes in terms of M and N. Assume ping is in O-(1)
 * and returns an int.
 */
public class Ex02 {
    public static void main(String[] args) {
        int M = 0;
        int N = 0;
        int j = 0;
        for (int i = N; i > 0; i--) {
            for (; j <= M; j++) {
            }
        }
    }
}
